import { TypingGameScreen } from '@/components/typing-game/typing-game-screen';

/** Standalone practice route (still the same screen as the entry point). */
export default function TypingGamePage() {
  return <TypingGameScreen />;
}
